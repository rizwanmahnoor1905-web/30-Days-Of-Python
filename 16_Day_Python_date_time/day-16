#day -16 :30 days off python
  from datetime import datetime
now=datetime.now()
print(now)
  from datetime import datetime
now=datetime.now()
t=now.strftime("%m/%d/%Y, %H:%M:%S")
print(t)
  from datetime import datetime
date_string = "5 December, 2019"
print("date_string =", date_string)     
date_object = datetime.strptime(date_string, "%d %B, %Y")
print("date_object =", date_object) 
  from datetime import date, datetime
today= date(year=2026, month=7, day=6)
new_date=date(year=2027, month=1, day=1)
time_left=new_date - today
print(f"time left for new year {time_left}")
    from datetime import date, datetime
past= date(year=1970, month=1, day=1)
future=date(year=2027, month=1, day=1)
time_difference=future- past
print(f"time difference {time_difference}")
      # i can use datetime module to calculate my screen time
